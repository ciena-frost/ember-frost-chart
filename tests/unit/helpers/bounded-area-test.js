import { expect } from 'chai';
import { describe, it } from 'mocha';
import { boundedArea } from 'ember-frost-chart/helpers/bounded-area';

describe('Unit | Helper | bounded area', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = boundedArea(42);
    expect(result).to.be.ok;
  });
});

