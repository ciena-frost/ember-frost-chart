import { expect } from 'chai';
import { describe, it } from 'mocha';
import { linearTicks } from 'ember-frost-chart/helpers/linear-ticks';

describe('Unit | Helper | linear ticks', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = linearTicks(42);
    expect(result).to.be.ok;
  });
});

