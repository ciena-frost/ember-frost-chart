import { expect } from 'chai';
import { describe, it } from 'mocha';
import { gradientArea } from 'ember-frost-chart/helpers/gradient-area';

describe('Unit | Helper | gradient area', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = gradientArea(42);
    expect(result).to.be.ok;
  });
});

