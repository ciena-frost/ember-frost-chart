import { expect } from 'chai';
import { describe, it } from 'mocha';
import { verticalBar } from 'ember-frost-chart/helpers/vertical-bar';

describe('Unit | Helper | vertical bar', function() {
  // Replace this with your real tests.
  it('works', function() {
    let result = verticalBar(42);
    expect(result).to.be.ok;
  });
});

