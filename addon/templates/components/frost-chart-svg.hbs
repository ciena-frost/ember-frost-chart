{{! Template for the frost-chart-svg component }}

{{#if chartState.axes.initialized}}
  {{yield
    (hash
      area=(component 'frost-chart-svg-plot-area'
        area=(range-bounded-area)
        chartState=chartState
        hook=(concat hookPrefix '-area')
      )

      bar=(component 'frost-chart-svg-plot-bar'
        bar=(vertical-bar)
        chartState=chartState
        hook=(concat hookPrefix '-bar')
      )

      stacked-bar=(component 'frost-chart-svg-plot-stacked-bar'
        bar=(vertical-bar)
        chartState=chartState
        hook=(concat hookPrefix '-stacked-bar')
      )

      x-grid=(component 'frost-chart-svg-plot-x-grid'
        chartState=chartState
        hook=(concat hookPrefix '-xGrid')
        ticks=(linear-ticks 10)
      )

      y-grid=(component 'frost-chart-svg-plot-y-grid'
        chartState=chartState
        hook=(concat hookPrefix '-yGrid')
        ticks=(linear-ticks 10)
      )

      line=(component 'frost-chart-svg-plot-line'
        chartState=chartState
        hook=(concat hookPrefix '-line')
        line=(d3-line)
      )

      scatter=(component 'frost-chart-svg-plot-scatter'
        chartState=chartState
        hook=(concat hookPrefix '-scatter')
      )
    )
  }}
{{/if}}
